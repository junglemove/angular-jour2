/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighlightRemoveDirective } from './highlight-remove.directive';

describe('HighlightRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
