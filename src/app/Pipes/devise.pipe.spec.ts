/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DevisePipe } from './devise.pipe';

describe('DevisePipe', () => {
  it('create an instance', () => {
    const pipe = new DevisePipe();
    expect(pipe).toBeTruthy();
  });
});
